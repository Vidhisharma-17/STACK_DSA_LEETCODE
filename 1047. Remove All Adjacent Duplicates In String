class Solution {
public:
    string removeDuplicates(string s) {
        string result;

        for(char y: s){
            if(!result.empty() && result.back()==y){
                result.pop_back();
            }else{
                result.push_back(y);
            }
        }
        return result;
    }
};
